{
  "name": "@nexus-engine/scripts",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Nexus Engine dev/build/release/liveops scripts (Bun + TypeScript).",
  "license": "MIT",
  "scripts": {
    "test": "bun test",
    "check": "bun run bin/check.ts",
    "lint": "bun x biome check ."
  },
  "devDependencies": {
    "@types/bun": "^1.1.13",
    "typescript": "^5.6.3"
  }
}
